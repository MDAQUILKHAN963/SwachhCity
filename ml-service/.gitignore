__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv
*.pt
*.onnx
*.pkl
*.h5
# Trained model WEIGHTS folder only (anchored to ml-service root with a leading
# slash). It must stay anchored so it does NOT match app/models/, which holds the
# ML model SOURCE code that the service imports.
/models/
dataset/
# Training artifacts (dataset copies, downloads, logs)
downloads/
trashnet_cls/
train.err
*.log
.DS_Store

ml_service.err
ml_service.log
